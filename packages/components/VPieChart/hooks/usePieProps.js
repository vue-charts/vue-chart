export default function () {
    let props = {
        data: Array,
        hiddenZeroPie: Boolean,
        hiddenLabel: Boolean,
        centerLabel: Boolean,
        radius: String | Array,
        roseType: Boolean
    }
    return props
}