export default function () {
    let props = {
        xAxis: Object,
        yAxis: Object,
        legend: Object,
        data: Array,
        stack: String,
        step: Boolean,
        area: Boolean,
        smooth: Boolean
    }
    return props
}